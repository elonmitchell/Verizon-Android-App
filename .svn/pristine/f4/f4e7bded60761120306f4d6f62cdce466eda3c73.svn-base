// Jobs Collection
// ===================

// Includes file dependencies
define([
	"jquery",
	"backbone",
	"../models/Job" ], function( $, Backbone, JobModel ) {

    // Extends Backbone.Router
    var Collection = Backbone.Collection.extend( {

        // The Collection constructor
        initialize: function( ) {

        },

        model: JobModel
    } );

    // Returns the Model class
    return Collection;

} );