// AccountList Model
// ==============

// Includes file dependencies
define([
	"jquery",
	"backbone"
], function( $, Backbone ) {

    // The Model constructor
    var AccountListModel = Backbone.Model.extend( {
		defaults: {
			phoneNumbers: []
		}
    } );

    // Returns the Model class
    return AccountListModel;

} );